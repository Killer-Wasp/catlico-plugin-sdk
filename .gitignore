.venv/
.pytest_cache/
__pycache__/
*.py[cod]

# Example plugins are libraries, not deployed apps — don't commit their lock
# files, matching catlico-plugins' convention for plugin repos.
examples/*/uv.lock

# Local environment
.env
.env.*
!.env.example
